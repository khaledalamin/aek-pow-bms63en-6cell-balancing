# Full Project Placeholder

This directory intentionally does not contain the complete ST-generated AutoDevKit / SPC5Studio project.

## Why

The original demo project, generated source, components, libraries, documentation, build outputs, and tooling files may be governed by ST licenses or third-party licenses. Do not publish the complete generated project unless your license explicitly allows redistribution.

## Recommended User Instructions

1. Install ST AutoDevKit / SPC5Studio and the required AEK-POW-BMS63EN / SPC58EC packages.
2. Recreate or import the ST BMS chain demo from official ST sources.
3. Build the original demo once.
4. Apply the modified file from:

```text
firmware/modified_files/AEK_POW_BMS63CHAIN_app_mng.c
```

5. Rebuild, flash, and validate with the testing protocol.

## Optional Local Use

For local private work, you may keep a complete generated project here if your license and workflow allow it. The repository `.gitignore` is configured to avoid committing build outputs and common generated artifacts.
